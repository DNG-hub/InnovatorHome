{
  "key": "valeska-evolution-25",
  "kind": "article",
  "locale": "en",
  "slug": "making-automated-capture-safer",
  "title": "Capture Must Not Record Everything",
  "description": "I am narrowing automated capture, redacting obvious secrets, and keeping runtime preparation from blocking other work. Convenience is not a reason to retain or stall.",
  "body": "<p>Automated capture is useful only when it preserves evidence without turning every tool interaction into retained material. Today I narrow that responsibility. The capture hook still excludes failed tool calls, talks to the FastAPI memory endpoint directly, and redacts several obvious forms of secret-bearing content before capture. At the same time I move the classifier’s runtime preparation off FastAPI’s event loop. The question is the same in both places: can the system record useful work without making capture itself a source of misleading records, exposed values, or stalled requests?</p><h2>The wrong default</h2><p>The usual automatic path is to keep everything. Failed calls, configuration files, credentials, a slow subprocess that is only asking whether a model is ready — all of it goes into memory because it was convenient to capture. Failed calls often describe an attempt rather than a completed action. Retaining them as ordinary memory can invent a history of success. A path can point at a file that should never be copied into a record. A blocking subprocess can delay unrelated API work while it checks a local runtime.</p><p>A diary that copies every scrap from the desk is not a diary. It is a leak. Each condition needs a specific control. Convenience is not enough reason to retain or block.</p><h2>Narrower rules on the hook</h2><p>The hook’s HTTP path now sends its capture payload through the documented FastAPI memory endpoint using a lightweight standard-library request. That removes a dead client import from the path and leaves the direct service import as a fallback when the API is unavailable. Capture content is prepared before it is persisted. Patterns cover environment-style secret assignments, bearer authorization values, AWS access-key forms, GitHub personal-access-token forms, and OpenAI-style key forms. When a path itself signals sensitive material — common environment, password, key, or credential files — the hook records only the action and path and suppresses the content or response preview.</p><p>These are controls for recognizable cases, not a claim of universal secret detection. A value outside the listed patterns can still need careful handling, and a filename rule is only as strong as the path the hook is given. Redaction happens before the capture request, so the ordinary persistence path is not asked to clean up a value after it has already received it. The hook also retains a reusable fallback service instance rather than repeatedly constructing and closing the expensive direct-import path. The exclusion of failed tool calls is not new today. It stays.</p><h2>Classification should not stall the API</h2><p>Runtime preparation can take far longer than an ordinary request: it may need the shared manager to select a model, claim GPU memory, and start a local runtime. The classifier still needs that preparation. It should not freeze FastAPI’s asynchronous event loop while the blocking work happens. I am moving the preflight from <a href=\"/en/blog/preparing-the-classification-runtime/\">yesterday’s runtime work</a> into a worker thread with <code>asyncio.to_thread</code>. Other in-flight requests can continue while the classifier waits. This does not make an unavailable model available. It protects the service’s responsiveness while the existing preflight reports the actual state.</p><h2>The next test</h2><p>Today’s change records an end-to-end check of the hook path from standard input through HTTP capture to the database, with the configured redactions applied, along with a Docker rebuild and eight task tests. That is evidence for the paths exercised, not a guarantee that every caller, secret form, or runtime condition has been covered. The next test should use representative safe fixtures to confirm that failed calls remain excluded, that each redaction class reaches persistence only as a marker, and that a slow or unavailable runtime leaves unrelated API requests responsive. The next review should also look for new secret formats and capture sources, because the rules have to follow the work they actually receive.</p><hr><p><strong>Historical basis:</strong> VALESKA commit <code>895579c</code>, May 18, 2026.</p>",
  "data": {
    "author": "David Gargan",
    "category": "VALESKA",
    "topic": "valeska",
    "tags": [
      "VALESKA",
      "enterprise memory",
      "knowledge architecture"
    ],
    "coverImage": "/images/articles/valeska-making-automated-capture-safer.webp",
    "coverAlt": "A worktable of papers passing under a lamp that blacks out a credential before the record is kept: capture that preserves evidence without copying every secret or stalling the rest of the house.",
    "historicalBasis": {
      "date": "2026-05-18",
      "type": "repository milestone",
      "reference": "VALESKA commit 895579c"
    }
  },
  "status": "published",
  "reviewed": true,
  "source_revision": 3,
  "published_at": "2026-05-18T12:00:00Z"
}
