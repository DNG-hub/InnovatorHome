Astro replacement: run ./docker-build.ps1 after npm ci and database setup.
The script validates the site, builds the local image, and updates localhost:3204.
It never pushes to a registry or connects a tunnel.
See README.md and docs/astro-migration.md for content instructions.
