# Copy to .env and generate a unique password. Never commit .env.
POSTGRES_PASSWORD=
DATABASE_URL=postgresql://avanti:REPLACE@127.0.0.1:5468/avanti
SITE_URL=http://localhost:3204
PORT=3204
# Set only for a read-only import from the legacy database:
# LEGACY_DATABASE_URL=
